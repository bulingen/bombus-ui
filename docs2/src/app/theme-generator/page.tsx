"use client";

import { useState } from "react";
import { FullThemeHex } from "../../../../tw-plugin/src/themes/types";

const Page = () => {
  const [currentTheme, setCurrentTheme] = useState<FullThemeHex>();

  return (
    <div>
      <div></div>
    </div>
  );
};

export default Page;
